interface:
  display_name: "Figma Variable Extract"
  short_description: "Figmaで使用中のVariablesからCSSトークンを生成"
  default_prompt: "Use $figma-variable-extract to extract Variables actually used by this Figma Design file, resolve alias dependencies, and generate validated CSS design tokens."
